# Окружение стенда. Python 3.10.
#
# torch ставится отдельно, из индекса под CUDA:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
# Обычный PyPI на Windows отдаёт сборку без CUDA.

# фундаментальные модели: SAM 3, DINOv3, Grounding DINO, OWLv2, Depth Anything v2
transformers>=5.15
accelerate
safetensors
timm

# без kernels SAM 3 пропускает удаление дублей, заливку дыр и чистку крапа в масках.
# Сборок под Windows у пакета нет: локально он ставится, но не применяется.
# На Linux применяется, проверено прогоном в контейнере. Два условия:
# доступ к хранилищу моделей при первом расчёте (скачивается 1,4 МБ) и
# невыключенная обезличенная статистика хранилища. При HF_HUB_DISABLE_TELEMETRY=1
# пакет собирает недопустимый заголовок запроса, проверка издателя падает,
# и обработка контуров пропускается молча. Подробности в docs/40-perenos-stenda.md.
kernels

# работа с изображениями и видео
pillow
opencv-python
imageio
imageio-ffmpeg

# визуализация: рамки, маски, номера треков
supervision
matplotlib

# данные Waymo v2 — колоночный parquet
pandas
pyarrow
dask[dataframe]

# метрики и карты эмбеддингов
numpy
scikit-learn

# ноутбуки
jupyterlab
ipywidgets
tqdm
